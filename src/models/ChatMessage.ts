// Backward compatibility — existing code that imports ChatMessage continues to work
export { Chat as ChatMessage } from './Chat';
export type { IChat as IChatMessage, ChatMessageEntry } from './Chat';
